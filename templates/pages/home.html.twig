{% extends "layouts/app.html.twig" %}

{% block title %}{{ parent() }} | Solutions Engineer{% endblock %}

{% block main %}
    {# Hero Section #}
    <div class="flex flex-col md:flex-row gap-10 md:gap-20 w-full items-center justify-center">
        <div class="w-full md:w-3/5 flex flex-col items-center md:items-start justify-center gap-6 grow">
            <p class="text-center md:text-left capitalize text-text text-4xl md:text-5xl lg:text-[42pt]">
                Web solutions to help your business run smarter.
            </p>

            <p class="text-text text-center md:text-left text-lg md:text-xl">
                Howdy! I'm a full-stack software engineer with a passion for creating and developing
                solutions for a variety of problems.
            </p>

            <div class="flex flex-col-reverse pt-2 md:pt-10 md:flex-row items-center justify-start gap-6">
                <a class="h-fit w-fit" href="https://www.github.com/krbeasley" target="_blank">
                    {% include "components/ui/github-icon.html.twig" with {
                        'class' : 'invert-80 hover:invert-100 duration-100 transition cursor-pointer h-10 w-10'
                    } %}
                </a>

                {% include "components/ui/glass-button.html.twig" with {
                    'text' : "Let's Work Together",
                    'href' : '/hire-me',
                    'frosted' : true,
                } %}
            </div>
        </div>

        <div class="w-fit items-center justify-end">
            <img src="/public/images/me_pfp.jpg" alt="Me!"
                class="mx-auto object-cover w-full max-w-[300px] h-100 md:w-[340px] md:h-[340px] lg:w-[550px]
                lg:h-[450px] rounded-lg">
        </div>
    </div>

    {% include "components/ui/divider.html.twig" %}

    {# "About" / Mission Statement #}
    <div class="flex flex-col gap-10 md:gap-20 w-full items-center justify-center px-4">
        <p class="text-text text-lg text-center">
            I'm Kyle, a full stack web developer who helps small to medium-sized businesses turn messy processes into
            smooth systems. <i class="text-primary">I don't just build websites, I create tools that solve problems.</i> Whether that's cutting
            down on wasted admin time, improving your online presence, or making it easier for customers to find and
            trust you, my goal is simple: make technology work for your business, not the other way around!
        </p>

        {% include "components/ui/glass-button.html.twig" with {
            'text' : "More About Me",
            'href' : '/about',
        } %}
    </div>

    {# Service Card Previews #}
    {% if service_cards is defined %}
        {# Extra Divider for this section #}
        {% include "components/ui/divider.html.twig" %}

        {# Services Preview Section #}
        <div class="flex flex-col gap-10 md:gap-16 w-full items-center justify-center px-4">
            <p class="text-primary text-2xl md:text-4xl lg:text-[36pt]">Services</p>

            <div class="flex flex-col lg:flex-row gap-10 md:gap-12 items-center justify-center">
                {% for card in service_cards %}
                    {% include "components/home-service-card.html.twig" with {
                        "card_title" : card["card_title"],
                        "card_text" : card["card_text"],
                        "img_path" : card["img_path"],
                        "img_alt" : card["img_alt"],
                        "button_link" : card["button_link"],
                        "button_text" : card["button_text"],
                    } %}
                {% endfor %}
            </div>
        </div>
    {% endif %}

    {# Todo: You should figure out more content to put here #}
{% endblock %}
